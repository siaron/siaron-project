spring:
  application:
    name: spring-cloud-example-gateway
  cloud:
    gateway:
      enabled: true

server:
  port: 8762

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/


logging:
  level:
    root: info